import { useMemo, useState } from "react";
import { ArrowRight, CalendarDays, FileText, Search, ShieldCheck } from "lucide-react";
import { Link } from "react-router-dom";
import { informationItems } from "@/data/site";

export default function InformationLibrary() {
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => informationItems.filter((item) => `${item.title} ${item.type} ${item.summary}`.toLowerCase().includes(query.toLowerCase())), [query]);
  return <section className="container py-12 md:py-16"><div className="max-w-3xl"><Link to="/information-centre" className="text-sm font-semibold text-slate-500 transition-colors hover:text-secondary">Information centre overview</Link><p className="mt-7 text-xs font-bold uppercase tracking-[0.22em] text-primary">Document library</p><h1 className="mt-3 font-display text-4xl font-bold leading-tight text-secondary md:text-5xl">Official information, organised for use.</h1><p className="mt-5 text-base leading-7 text-slate-600">Browse the publication structure for school documents, circulars, calendars, policies, and approved guidance. Each record has its own readable route so families can share or return to a specific publication.</p></div><label className="mt-8 flex max-w-xl items-center gap-3 rounded-xl border border-input bg-card px-4 py-3 shadow-sm transition-shadow focus-within:shadow-[var(--shadow-elevated)]"><Search className="h-5 w-5 text-slate-400" /><span className="sr-only">Search documents</span><input aria-label="Search documents" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search documents and categories" className="w-full bg-transparent text-sm outline-none" /></label><div className="mt-8 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{filtered.map((item) => { const Icon = item.icon; return <Link key={item.slug} to={`/information-centre/documents/${item.slug}`} className="group rounded-xl border border-border bg-card p-5 shadow-sm transition-all duration-200 hover:-translate-y-1 hover:border-primary/60 hover:shadow-[var(--shadow-elevated)] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"><div className="flex items-start justify-between gap-4"><span className="rounded-lg bg-primary/15 p-2.5 text-secondary"><Icon className="h-5 w-5" /></span><ArrowRight className="h-4 w-4 text-slate-400 transition-transform duration-200 group-hover:translate-x-1 group-hover:text-secondary" /></div><p className="mt-5 text-[11px] font-bold uppercase tracking-[0.16em] text-slate-500">{item.type}</p><h2 className="mt-2 font-display text-xl font-bold leading-snug text-secondary">{item.title}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{item.summary}</p><span className="mt-5 inline-flex text-xs font-bold uppercase tracking-wider text-secondary group-hover:text-primary">Read publication <ArrowRight className="ml-2 h-3.5 w-3.5" /></span></Link>; })}</div>{filtered.length === 0 && <div className="mt-8 rounded-xl border border-dashed border-border p-8 text-center"><ShieldCheck className="mx-auto h-7 w-7 text-primary" /><p className="mt-3 text-sm font-semibold text-secondary">No matching information found.</p></div>}<div className="mt-12 grid gap-4 border-t border-border pt-8 md:grid-cols-3"><div className="flex gap-3"><FileText className="h-5 w-5 shrink-0 text-primary" /><p className="text-sm leading-6 text-slate-600"><strong className="text-secondary">Versioned records.</strong> Each document should show its publication and review information.</p></div><div className="flex gap-3"><CalendarDays className="h-5 w-5 shrink-0 text-primary" /><p className="text-sm leading-6 text-slate-600"><strong className="text-secondary">Dated notices.</strong> Families can distinguish current guidance from archived information.</p></div><div className="flex gap-3"><ShieldCheck className="h-5 w-5 shrink-0 text-primary" /><p className="text-sm leading-6 text-slate-600"><strong className="text-secondary">Responsible publication.</strong> Official school offices maintain the final record.</p></div></div></section>;
}
