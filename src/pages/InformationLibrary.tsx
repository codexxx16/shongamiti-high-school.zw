import { useMemo, useState } from "react";
import { CalendarDays, FileText, Search, ShieldCheck } from "lucide-react";
import { Link } from "react-router-dom";
import { informationItems } from "@/data/site";

export default function InformationLibrary() {
  const [query, setQuery] = useState("");
  const filtered = useMemo(() => informationItems.filter((item) => `${item.title} ${item.type}`.toLowerCase().includes(query.toLowerCase())), [query]);
  return <section className="container py-16 md:py-24"><div className="max-w-3xl"><Link to="/information-centre" className="text-sm font-semibold text-slate-500 hover:text-secondary">Information centre overview</Link><p className="mt-8 text-xs font-bold uppercase tracking-[0.22em] text-primary">Document library</p><h1 className="mt-4 font-display text-5xl font-bold text-secondary">Official information, organised for use.</h1><p className="mt-6 text-lg leading-8 text-slate-600">Browse the publication structure for school documents, circulars, calendars, policies, and approved guidance. Documents will be added and versioned by the responsible school office.</p></div><div className="mt-10 flex max-w-xl items-center gap-3 rounded-xl border border-input bg-card px-4 py-3 shadow-sm"><Search className="h-5 w-5 text-slate-400" /><input aria-label="Search documents" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search documents and categories" className="w-full bg-transparent text-sm outline-none" /></div><div className="mt-10 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{filtered.map((item) => { const Icon = item.icon; return <article key={item.title} className="rounded-2xl border border-border bg-card p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-[var(--shadow-elevated)]"><div className="flex items-center justify-between"><span className="rounded-xl bg-primary/15 p-3 text-secondary"><Icon className="h-5 w-5" /></span><span className="text-xs font-bold uppercase tracking-wider text-slate-500">{item.type}</span></div><h2 className="mt-7 font-display text-2xl font-bold text-secondary">{item.title}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{item.date}. The published version will appear here when approved by the responsible office.</p><button type="button" className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-secondary" onClick={() => window.alert("This document is prepared for publication in the school information centre.")}>View publication record <FileText className="h-4 w-4 text-primary" /></button></article>; })}</div>{filtered.length === 0 && <div className="mt-10 rounded-2xl border border-dashed border-border p-10 text-center"><ShieldCheck className="mx-auto h-8 w-8 text-primary" /><p className="mt-4 font-semibold text-secondary">No matching information found.</p></div>}<div className="mt-14 grid gap-5 border-t border-border pt-10 md:grid-cols-3"><div className="flex gap-3"><FileText className="h-5 w-5 shrink-0 text-primary" /><p className="text-sm leading-6 text-slate-600"><strong className="text-secondary">Versioned records.</strong> Each document should show its publication and review information.</p></div><div className="flex gap-3"><CalendarDays className="h-5 w-5 shrink-0 text-primary" /><p className="text-sm leading-6 text-slate-600"><strong className="text-secondary">Dated notices.</strong> Families should be able to distinguish current guidance from archived information.</p></div><div className="flex gap-3"><ShieldCheck className="h-5 w-5 shrink-0 text-primary" /><p className="text-sm leading-6 text-slate-600"><strong className="text-secondary">Responsible publication.</strong> Official school offices maintain the final record.</p></div></div></section>;
}
