import { ArrowUpRight, CheckCircle2, ChevronRight } from "lucide-react";
import { Link } from "react-router-dom";
import type { PortalPageData } from "@/data/site";

const focusable = "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2";

export function PortalPage({ data }: { data: PortalPageData }) {
  const action = data.eyebrow === "Admissions" ? { href: "/admissions/apply", label: "Start an application" } : data.eyebrow === "The school" ? { href: "/school/leadership", label: "Meet school leadership" } : data.eyebrow === "Academic life" ? { href: "/academics/curriculum", label: "Read the curriculum guide" } : data.eyebrow === "Student life" ? { href: "/student-life/sports", label: "Explore student life" } : { href: "/information-centre/documents", label: "Visit the document centre" };
  return <div><section className="hero-gradient relative overflow-hidden text-secondary-foreground"><div className="absolute -right-24 top-16 h-72 w-72 rounded-full border border-primary/20 bg-primary/10 blur-3xl" /><div className="container relative grid gap-9 py-14 md:grid-cols-[1.2fr_0.8fr] md:items-end md:py-20"><div><p className="mb-4 text-xs font-bold uppercase tracking-[0.25em] text-primary">{data.eyebrow}</p><h1 className="max-w-4xl font-display text-3xl font-bold leading-[1.08] md:text-5xl">{data.title}</h1><p className="mt-5 max-w-2xl text-sm leading-7 text-secondary-foreground/75 md:text-base">{data.intro}</p></div><div className="border-l border-primary/30 pl-5 md:mb-2"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-primary">Official school portal</p><p className="mt-3 text-sm leading-6 text-secondary-foreground/70">Read the overview, follow the related routes, and use the practical links to move from information to action.</p></div></div></section><div className="border-b border-border bg-card"><div className="container flex flex-wrap items-center gap-2 py-2.5 text-xs font-semibold text-slate-500"><Link to="/" className={`transition-colors duration-200 hover:text-secondary ${focusable}`}>Home</Link><ChevronRight className="h-3.5 w-3.5" /><span className="text-secondary">{data.eyebrow}</span></div></div><section className="container grid gap-10 py-12 md:grid-cols-[0.72fr_1.28fr] md:py-16"><aside className="h-fit md:sticky md:top-28"><p className="text-xs font-bold uppercase tracking-[0.2em] text-primary">In this section</p><div className="mt-4 grid gap-1.5 border-l border-border pl-4 text-sm font-semibold text-slate-600">{data.sections.map((section) => { const id = section.title.toLowerCase().replace(/[^a-z0-9]+/g, "-"); return <a key={section.title} href={`#${id}`} className={`transition-colors duration-200 hover:text-secondary ${focusable}`}>{section.title}</a>; })}</div><Link to={action.href} className={`btn-gold mt-7 inline-flex items-center gap-2 text-sm ${focusable}`}>{action.label}<ArrowUpRight className="h-4 w-4" /></Link></aside><div className="space-y-9">{data.sections.map((section) => { const id = section.title.toLowerCase().replace(/[^a-z0-9]+/g, "-"); return <article key={section.title} id={id} className="scroll-mt-28 border-b border-border pb-9 last:border-0"><h2 className="font-display text-2xl font-bold text-secondary md:text-3xl">{section.title}</h2><p className="mt-4 text-sm leading-7 text-slate-600 md:text-base">{section.body}</p>{section.items && <div className="mt-5 grid gap-3 sm:grid-cols-2">{section.items.map((item) => <div key={item} className="flex items-start gap-3 rounded-lg bg-muted/60 p-3.5 text-sm leading-6 text-slate-700 transition-colors duration-200 hover:bg-primary/10"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-primary" />{item}</div>)}</div>}</article>; })}</div></section>{data.cards && <section className="bg-muted/60 py-12 md:py-16"><div className="container"><p className="text-xs font-bold uppercase tracking-[0.2em] text-primary">Continue exploring</p><div className="mt-5 grid gap-4 md:grid-cols-3">{data.cards.map((card) => { const Icon = card.icon; return <Link key={card.title} to={card.href} className={`group rounded-xl border border-border bg-card p-5 shadow-sm transition-all duration-200 hover:-translate-y-1 hover:border-primary/60 hover:shadow-[var(--shadow-elevated)] ${focusable}`}><div className="flex items-start justify-between gap-4"><span className="rounded-lg bg-primary/15 p-2.5 text-secondary"><Icon className="h-5 w-5" /></span><ArrowUpRight className="h-4 w-4 text-slate-400 transition-transform duration-200 group-hover:-translate-y-0.5 group-hover:translate-x-0.5 group-hover:text-secondary" /></div><h3 className="mt-6 font-display text-xl font-bold text-secondary">{card.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{card.description}</p><span className="mt-4 inline-flex items-center gap-2 text-xs font-bold uppercase tracking-wider text-secondary group-hover:text-primary">Open section <ArrowUpRight className="h-3.5 w-3.5" /></span></Link>; })}</div></div></section>}</div>;
}
